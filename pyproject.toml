[project]
name = "litterbug"
version = "0.1.0"
description = "Waste detection and instance segmentation on a conveyor belt (BUU Waste Occlusion)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "opencv-python",
    "tqdm",
    "ultralytics",
]

[project.scripts]
litterbug = "litterbug.cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# One installable package, so nothing lands in site-packages as a top-level name.
# The concerns are subpackages of it: one folder per concern, under src/litterbug/.
[tool.hatch.build.targets.wheel]
packages = ["src/litterbug"]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.uv]
package = true

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
